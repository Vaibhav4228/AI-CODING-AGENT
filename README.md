# my-codex backend

```powershell
cd backend
npm install
copy env.example .env
npm run dev
```

API: `http://localhost:5000`

JEV sidecar (optional): `cd ../services/jev-gateway && python main.py` → `:8001`
